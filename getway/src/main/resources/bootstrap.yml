# 网关动态路由配置放到nacos中，getway-application
spring:
  application:
    name: getway-service
  cloud:
    nacos:
      config:
        server-addr: home.mytest.com:8848
        username: nacos
        password: nacos
        file-extension: yml
        name: getway-application
      discovery:
        secure: false